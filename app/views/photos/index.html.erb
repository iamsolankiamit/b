<% content_for :body do %>
<div class="container_24 ">
  <div class="grid_24 edit-offer-container">
    <div class="grid_5 alpha omega offer-side-menu-content">
      <ul>
        <li class="blank"></li>
        <li><%= link_to "Dashboard", offers_path %></li>
        <li class="active"><%= link_to "My Listings", offers_path %></li>
        <li><%= link_to "Profile settings", edit_user_registration_path %></li>
      </ul>
    </div>

    <div class="grid_19 alpha omega offer-edit-main-content">
      <div class="grid_19">
        <div class="submenu">
          <ul>
            <li><a href="">Overview</a></li>
            <li ><%= link_to "Title & Description", edit_offer_translations_path %></li>
            <li><%= link_to "Details", edit_offer_details_path %></li>
            <li><%= link_to "Address", edit_offer_addresses_path %></li>
            <li class="active"><%= link_to "Photos", offer_photos_path %></li>
            <li><%= link_to "Pricing & Terms", edit_offer_pricing_path %></li>
          </ul>
        </div>
        <div class="title"><h2>Photos</h2>
<p>Snap it so that the guest gets a better idea.
Don’t put random photos to confuse our guests. </p>
        </div>
        <hr class=" offer-hr">


<ul class="thumbnails">
<% @photos.each do |photo| %>
  <li class="span2">
    <div class="thumbnail">
<%= image_tag photo.image.url(:thumb) %>
      <%= link_to "Edit |", edit_offer_photo_path(photo.offer_id,photo), class: "disabled" %>
      <%= link_to " Delete", offer_photo_path(photo.offer_id,photo), :method => "delete"  %>
    </div>
  </li>
<% end %>
</ul>

 <div class="container photos">
<%= form_tag offer_photos_path, :method => :post, :multipart => true, :id => "fileupload" do %>

    <!-- The fileupload-buttonbar contains buttons to add/delete files and start/cancel the upload -->
    <div class="fileupload-buttonbar">
        <!-- The fileinput-button span is used to style the file input field as button -->
        <span class="btn btn-success fileinput-button">
          <i class="icon-plus icon-white"></i>
          <span>Add files...</span>
  
<%= file_field_tag :image, :name => "photo[image]" , :multiple => true %>


        </span>

        <button type="submit" class="btn btn-primary start">
          <i class="icon-upload icon-white"></i>
          <span>Start upload</span>
        </button>
        <button type="reset" class="btn btn-warning cancel">
          <i class="icon-ban-circle icon-white"></i>
          <span>Cancel upload</span>
        </button>
        <button type="button" class="btn btn-danger delete">
          <i class="icon-trash icon-white"></i>
          <span>Delete</span>
        </button>
        <input type="checkbox" class="toggle">
      </div>
      <div class="">
        <!-- The global progress bar -->
        <div class="progress progress-success progress-striped active fade">
          <div class="bar" style="width:0%;"></div>
        </div>
      </div>
    <!-- The loading indicator is shown during image processing -->
    <div class="fileupload-loading"></div>
    <br>
    <!-- The table listing the files available for upload/download -->
    <table class="table table-striped"><tbody class="files" data-toggle="modal-gallery" data-target="#modal-gallery"></tbody>
    </table>
  <% end %>

</div>
      </div>
    </div>
  </div>
</div>

<% end %>