<% content_for :header do %>
<div class="search-container">
  <div class="title" style="height:100px">Travelers Accomodation, simplified.</div>
  <div class="flood-callout" style="text-align:center;color:#ecf0f1">
<div>
    <h2 style="font-size:2.5em;
font-weight: 700;
text-shadow: 0px 0px 10px #2c3e50;">Help the Flood Victims</h2>
    <%= link_to "Provide Shelter To Flood Victims <i class='icon-heart icon-white'></i> ".html_safe, new_offer_path,  class: "btn btn-large" %>
    <%= link_to "Are You A Flood Victim?",floods_path,  class: "btn btn-large" %>
    </div>
  </div>
  <div class="search">
    <%= form_tag "/search", :method => 'get', id: 'search_form' do %>
    <%= text_field_tag :location, nil, placeholder: "Where do you want to go?", style: "width: 250px;margin-left: 5px;" %>
    <%= text_field_tag :time, nil, placeholder: "Check in" %>
    <%= text_field_tag :time, nil, placeholder: "Check out" %>
    <%= text_field_tag :guests, nil, placeholder: "Guests" %>
    <%= submit_tag :search, class: "btn btn-danger btn-large" %>
    <% end %>
  </div>
</div>

<div class="listing-container">
  <div class="list-title">Latest Listings On RoomnHouse.</div>
  <div class="listings">
    <% @offers.each do |offer| %>
      
        <div class="item">
          
          <div class="image-container">
            <%= link_to offer_path(offer) do %>
            <% if offer.photos.exists? %>
              <% if !offer.photos.first.nil? %>
                <%= image_tag offer.photos.first.image.url(:homeimage)%>
              <% end %>
            <% end %>
          <% end %>
          </div>
          <div class="price"><span class="rs"><%= offer.nightly_rate_amount %></span><br/><span><%= offer.currency %>/night</span></div>
          <% if offer.translations.exists? %>
            <% if !offer.translations.first.title.nil? %>
              <div class="name"><%= truncate(offer.translations.first.title.titlecase, length: 30, separator: ' ') %></div>
            <% end %>
          <% else %>
            <div class="name">Offer (<%= offer.id %>).</div>
          <% end %>
          <div class="info"><%= offer.object_type.humanize %> &#8208; <%= offer.address_addon %></div>
        </div>
      
    <% end %>
  </div>
</div>
<% end %>