# For more information see: http://emberjs.com/guides/routing/

# Roomnhouse.Router.reopen location: "history"

# Roomnhouse.Router.map ()->
#  rootURL: '/'
#  @resource 'admin', path: '/admin/:id', ->
#    @resource 'users', path: '/users', queryParams: ['lister_id'], ->
#      @route 'new', path: '/new'
#      @resource 'user', path: '/:id'
#    @resource 'offers', path: '/offers', ->
#      @route 'new', path: '/new'
#      @resource 'offer', path: '/:id'
